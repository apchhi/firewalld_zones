# firewalld_zones

Павел, держи **готовый, чистый, профессиональный README.md**, который можно сразу класть в твой GitHub‑репозиторий.  
Он объясняет:

- какие зоны есть в firewalld  
- для чего каждая нужна  
- какая зона подходит для твоей архитектуры  
- как переключаться между ними  
- как проверить состояние  

Стиль — минималистичный, технический, понятный.

---

# 🛡️ Firewalld Zones — Полное руководство  
**Минималистичная и безопасная конфигурация для Fedora**

Этот документ описывает:

- какие зоны существуют в firewalld  
- для чего они предназначены  
- какую зону использовать на ноутбуке  
- как переключать интерфейсы между зонами  
- как проверить текущую конфигурацию  

Документ подходит для архитектуры:

- **clean / dirty / tor** браузерной изоляции  
- Firejail‑sandboxing  
- rootless контейнеров  
- VPN  
- безопасного Wi‑Fi использования  

---

# 📌 1. Что такое зоны firewalld

Зона — это набор правил, который применяется к сетевому интерфейсу.  
Каждый интерфейс (например, `wlp1s0`) может быть привязан к одной зоне.

---

# 📚 2. Основные зоны и их назначение

Ниже — только те зоны, которые реально важны.

---

## 🟥 **drop** — максимальная безопасность  
**Рекомендуется для ноутбука.**

- все входящие пакеты → DROP  
- исходящие → разрешены  
- компьютер полностью невидим в сети  
- никакие сервисы не открыты  
- идеален для Wi‑Fi, общественных сетей, мобильной работы  

Используется в нашей архитектуре.

---

## 🟧 **block**  
- входящие → REJECT (отвечает ICMP‑ошибкой)  
- исходящие → разрешены  
- компьютер виден в сети, но не принимает соединения  

---

## 🟦 **public**  
**Зона по умолчанию в Fedora.**

- предназначена для публичных сетей  
- разрешает некоторые сервисы (ssh, mdns и др.)  
- не подходит для максимальной безопасности  

Мы заменили её на drop.

---

## 🟩 **home / internal**  
- предназначены для доверенных сетей  
- разрешают mdns, samba, ssh  
- подходят для домашнего сервера  
- НЕ подходят для ноутбука  

---

## 🟪 **trusted**  
- разрешает всё  
- использовать нельзя  

---

# 🧭 3. Какая зона нужна в нашей архитектуре

Для твоей системы:

- Firejail изолирует браузеры  
- Tor/clean/dirty работают в отдельных песочницах  
- контейнеры rootless  
- ноутбук используется в Wi‑Fi сетях  

**Идеальная зона: `drop`**

Причины:

- максимальная безопасность  
- нулевая поверхность атаки  
- невидимость в сети  
- не ломает VPN  
- не ломает Tor  
- не ломает контейнеры  
- не ломает браузеры  

---

# 🔧 4. Как переключить интерфейс в нужную зону

## Проверить активные зоны:

```
sudo firewall-cmd --get-active-zones
```

---

## Переключить интерфейс в зону drop

```
sudo firewall-cmd --zone=drop --add-interface=wlp1s0
```

---

## Установить drop как зону по умолчанию

```
sudo firewall-cmd --set-default-zone=drop
```

---

## Удалить интерфейс из другой зоны (если нужно)

Если интерфейс уже в drop, firewalld покажет:

```
ZONE_CONFLICT
```

Это нормально — значит интерфейс уже перемещён.

---

# 🔍 5. Как проверить текущую конфигурацию

## Проверить default zone:

```
sudo firewall-cmd --get-default-zone
```

Ожидаем:

```
drop
```

---

## Проверить активные зоны:

```
sudo firewall-cmd --get-active-zones
```

Ожидаем:

```
drop
  interfaces: wlp1s0
```

---

## Проверить правила зоны drop:

```
sudo firewall-cmd --zone=drop --list-all
```

Ожидаем:

- services: *(пусто)*  
- ports: *(пусто)*  
- masquerade: no  
- forward: yes/ no (не важно)  

---

# 🧩 6. Как временно переключиться в другую зону

Иногда нужно:

- протестировать сервис  
- поднять локальный сервер  
- открыть порт для разработки  

## Временно переключить интерфейс:

```
sudo firewall-cmd --zone=home --add-interface=wlp1s0
```

## Вернуть обратно:

```
sudo firewall-cmd --zone=drop --add-interface=wlp1s0
```

---

# 🧼 7. Рекомендации по безопасности

- не использовать public/home/internal на ноутбуке  
- не открывать ssh, mdns, samba  
- не включать trusted  
- держать интерфейс в drop  
- использовать VPN при необходимости  
- использовать Firejail для приложений  

---

# 🟩 8. Итог

Твоя система теперь:

- полностью скрыта в сети  
- не принимает входящие соединения  
- не раскрывает hostname через mDNS  
- не открывает SSH  
- не даёт fingerprinting через сетевые сервисы  
- идеально сочетается с Firejail и Tor  

---

Павел, если хочешь, я могу:

- добавить раздел про **VPN + firewalld**  
- добавить раздел про **Tor‑only zone**  
- добавить раздел про **контейнерные сети**  
- собрать всё в единый большой README для репозитория  

Скажи, что включить.
